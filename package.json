{
  "name": "africybercore",
  "version": "1.0.0",
  "type": "module",
  "description": "AfriCyberCore - Static marketing website",
  "scripts": {
    "dev": "serve . -l 3000",
    "build": "mkdir -p dist && cp -r *.html assets dist/ && cp -r assets/* dist/assets/ 2>/dev/null || true",
    "preview": "npx serve . -l 3000",
    "start": "npx serve . -l 3000"
  },
  "dependencies": {},
  "devDependencies": {}
}
